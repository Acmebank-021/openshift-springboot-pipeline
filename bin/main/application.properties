# 8443 en vez de 8080 - mismo puerto que usan los dos Dockerfile reales vistos
# de Banamex. En su caso el TLS probablemente lo termina el Route de OpenShift,
# no la propia app - aqui se deja HTTP simple sobre ese puerto, no se agrega
# keystore/SSL porque no tenemos certificados reales que simular.
server.port=8443
